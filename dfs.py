from constants import GOAL_STATE

def depthFirstSearch(board):
    # frontier is initialized to a stack with the first board
    # explored is initialized as an empty set

    # while frontier is not empty
        # current state is set to the board at the top of the set
        # current state is added to the set of explored boards

         # if current state is GOAL_STATE
            # return success

        # for neighbor in neighbors of current board
            # if neighbor board is not already explored or in queue to be Explored
                # push the neighbor onto the stack (ensure neighbors are added in such a way that they are popped off in UDLR order)
    return 'yo'
